note over BROWSER
js code creates event handler that prevents GET method.
event handler also adds the new note to the list and renders it
and finally sends it to the server
end note

BROWSER->SERVER: HTTP POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa